/**
 * Bash process runner
 */

import { ProcessRunner } from './base.js';

/**
 * Bash/script process runner
 * Inherits all functionality from BaseRunner
 */
export class BashRunner extends ProcessRunner {
  // No overrides needed - uses base implementation
}
