project: minimal

processes:
  hello:
    command: echo "Hello from Orckit!"
    ready:
      type: exit-code

  world:
    command: echo "World!" && sleep 1
    depends_on: [hello]
    ready:
      type: exit-code
